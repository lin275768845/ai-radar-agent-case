name = "ai-radar-single-card-dispatcher"
main = "src/index.js"
compatibility_date = "2026-06-05"
preview_urls = false

# Public mirror pattern config only.
# Private production deployments must override settings outside this repo.
[vars]
GITHUB_OWNER = "lin275768845"
GITHUB_REPO = "ai-radar-agent-case"
GITHUB_WORKFLOW = "daily.yml"
GITHUB_REF = "main"
BOCHA_ENABLED = "false"

[triggers]
crons = ["0 2 * * *"]

[observability]
enabled = true
head_sampling_rate = 1
